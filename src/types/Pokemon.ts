export type Pokemon = {
    name: string,
    url: string
};

export type PokemonStats = {
    hp: number,
    attack: number,
    specialAttack: number,
    defense: number,
    specialDefense: number,
    speed: number
}